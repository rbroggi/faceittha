name: CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: running CI
      run: make ci
    - name: dump docker logs
      if: failure()
      uses: actions/upload-artifact@v1
      run: docker logs faceittha-debezium-1
